apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "strfry.fullname" . }}-test-connection"
  labels:
    {{- include "strfry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "strfry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
