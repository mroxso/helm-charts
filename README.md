# helm-charts

# Setup
1. Create a Helm Chart in this Repo
2. `helm package <package-name>`, then copy the created file to the same named folder
3. `helm repo index <package-name>` (this file is at the correct place already)