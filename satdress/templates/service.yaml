apiVersion: v1
kind: Service
metadata:
  name: {{ include "satdress.fullname" . }}
  labels:
    {{- include "satdress.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "satdress.selectorLabels" . | nindent 4 }}
